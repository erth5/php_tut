<?php

/*
class Report
{
    public function generate(¼user){
        test
    }
}
*/

# for example attack put: &greeting=&name=<a href="https://google.com">frank</a>
# on Browser line

/* Datebank ohne Struktur
function fetchAllTasks($pdo){
    $pdo = new PDO('mysql:host=127.0.0.1;dbname=test', 'root', '');
    $statement = $pdo->prepare('select * from test');
    $statement->execute();

    $db_res = ($statement->fetchAll(PDO::FETCH_OBJ));
    var_dump($db_res[0]->test);
    var_dump($db_res[0]->abfg);
    return $statement->fetchAll(PDO::FETCH_CLASS, 'test');
}
*/
